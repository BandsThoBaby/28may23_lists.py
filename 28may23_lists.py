
"""
Lists allow us to store multiple values in a  single variable. 

Lists are: Ordered | Changeable | Allow duplicate values 
"""

a_new_list = ["papaya", "mango", "pineapple"]
print(a_new_list)

# List items are indexed. (Starting at [0] of course!!!!)

# If you add new items to a list, they will be placed at the end of the list. 

# Because a list is changebale we can: change, add and remove values after a list has been created. 

# Since lists are index they allow items to have the same values. 

# We can make list items from any data type: Int, boolean, float... whatever you want...

string_list = ["Bangkok", "Buriram", "Phuket"]
int_list = [4, 6, 9, 2]
bool_list = [True, False, False]

# A list can also be created using the list() constructor:

july_plans = list(("Tate_Modern", "Magic_Mushrooms", 5000, True

))

print(july_plans)